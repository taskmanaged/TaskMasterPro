import XCTest
@testable import YourAppModule

class RecurrenceManagerTests: XCTestCase {

    func testRecurrenceManagerImport() {
        // This test simply verifies that the RecurrenceManager can be imported
        // and doesn't cause any compilation errors
        XCTAssertTrue(true, "RecurrenceManager should be importable")
    }

}
