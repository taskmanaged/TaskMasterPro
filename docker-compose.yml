services:
  backend:
    build: ./backend
    ports:
      - '5000:5000'
    environment:
      - PORT=5000
      - DB_HOST=database
      - DB_PORT=5432
      - DB_USER=taskmaster_user
      - DB_PASSWORD=securepassword
      - DB_NAME=taskmasterpro_dev
      - JWT_SECRET=nk90631a4xTfEEaxqdS+8Fgk/gfEHlWKtGA0zcPcM77pt86lweCyTF2pE1PPACpsKIPc3Jbei
    depends_on:
      - database
  frontend:
    build: ./frontend
    ports:
      - '3000:3000'
    depends_on:
      - backend
  database:
    image: postgres:13
    environment:
      - POSTGRES_USER=taskmaster_user
      - POSTGRES_PASSWORD=GUm8Teth
      - POSTGRES_DB=taskmasterpro_dev
    ports:
      - '5433:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
